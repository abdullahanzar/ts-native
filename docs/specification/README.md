# TS-Native Specification

This directory contains the versioned language specification for TS-Native.

## Versions
- `v0/`: normative baseline for the initial language core.

Each version is self-contained and chapter-oriented. Breaking changes should be introduced through a new version directory rather than rewriting prior versions in place.
